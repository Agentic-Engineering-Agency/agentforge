# Active Specs

As of 2026-03-10, GitHub issues are the canonical active backlog for AgentForge.

Reason:
- The prior `specs/active/` set had accumulated stale sprint docs, architecture documents that no longer matched the repo, and overlapping implementation notes that conflicted with the current daemon-first Mastra + Convex boundary.
- The backlog reset on 2026-03-10 rewrote the live GitHub issues into spec-quality artifacts and deleted obsolete issues that no longer matched project scope.

Current canonical issues:
- `#75` Dashboard Canonicalization
- `#73` Project Configuration Integrity
- `#72` Model Catalog And Selection UX
- `#74` Files Productization
- `#44` Skills Marketplace Productization
- `#15` MCP Lifecycle Completion
- `#38` Sandbox Execution Productization
- `#4` Cloudflare Deployment Modernization
- `#213` Runtime/CLI Convergence
- `#214` Dashboard E2E And Route Reliability
- `#215` Convex Runtime-Boundary Cleanup
- `#216` Project configuration bug: cannot select agents
- `#217` Chat-scoped model override
- `#218` Settings page hardcoded-state bug
- `#219` Modal outside-click dismissal bug

Archived backlog snapshot:
- `specs/archive/backlog-reset-2026-03-10/`

If a new local spec is needed again, it should either:
- directly support one of the canonical GitHub issues above, or
- replace this README with a new clearly owned active spec set.
