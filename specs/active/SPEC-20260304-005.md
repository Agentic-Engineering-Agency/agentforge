# SPEC-20260304-005: Integration QA & Session Detail Fix

**Status:** SPEC
**Created:** 2026-03-04
**Priority:** P0

## Summary

Fix the remaining broken surfaces found during full end-to-end testing: session detail crash, endSession deployment, and CLI sessions get mismatch. Then run complete integration tests across all recent specs.

## Requirements

- Fix `sessions.$sessionId.tsx` crashing because `api.sessions.endSession` is not found on deployed Convex. Verify the mutation is exported from `convex/sessions.ts` and re-deploy.
- Fix `sessions.$sessionId.tsx` routeTree: route id must be `/sessions/$sessionId`, path must be `/sessions/$sessionId`, getParentRoute must return `rootRouteImport`. Confirm by navigating to a real session URL and verifying the detail page (not the sessions list) renders.
- Verify `agentforge sessions get <id>` works by calling it against a real session `_id` from `agentforge sessions list`. It should print session details with message preview.
- Verify `agentforge sessions delete <id> --force` deletes a test session.
- Verify `agentforge threads rename <id> <name>` renames a thread.
- Verify `agentforge threads delete <id> --force` deletes a thread.
- Verify Settings > Tokens tab shows real tokens, Generate creates new token with one-time reveal, Revoke deactivates a token.
- Run `pnpm test` — all 911+ tests must pass.
- Run `npx tsc --noEmit` in `packages/web` — zero TypeScript errors.

## Acceptance Criteria

- `GET /sessions/:id` renders the session detail page with message history ✓
- `agentforge sessions get <real-_id>` prints session details ✓
- `agentforge sessions delete <id> --force` succeeds ✓
- `agentforge threads rename <id> <new-name>` succeeds ✓
- Settings > Tokens tab fully functional ✓
- All 911+ tests passing ✓
- Zero TypeScript errors ✓

## Implementation Notes

- Work in `/tmp/agentforge-team-a` on branch `feat/specs-001-002-v2`
- Deploy to `watchful-chipmunk-946`: `CONVEX_DEPLOYMENT=dev:watchful-chipmunk-946 npx convex dev --once`
- `sessions.endSession` is in `convex/sessions.ts` — ensure it's deployed
- For routeTree fix: match exact pattern from `RunsRunIdRoute` (which works correctly)
- Sync all `convex/*.ts` changes to `packages/cli/templates/default/convex/` AND `packages/cli/dist/default/convex/`
