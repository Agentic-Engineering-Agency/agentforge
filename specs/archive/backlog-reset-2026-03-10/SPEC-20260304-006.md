# SPEC-20260304-006: Agent Detail Page & Agents Page Polish

**Status:** SPEC
**Created:** 2026-03-04
**Priority:** P1

## Summary

Verify, fix, and polish the agent detail page at `/agents/:id` (implemented in SPEC-002). Wire the Sessions tab to real data, fix any route registration issues, and add missing Convex queries.

## Requirements

- Verify `/agents/agent-test-001` loads and shows the full agent detail page. If it crashes, fix it.
- The Overview tab must show: agent name, model, provider, temperature, maxTokens, instructions preview, failover chain.
- The Sessions tab must call `api.sessions.listByAgent` — create this query in `convex/sessions.ts` if missing: `args: { agentId: v.string() }`, returns sessions for that agent ordered by createdAt desc. Rows must link to `/sessions/:id`.
- The Settings tab must show an editable form pre-filled with agent data. Save calls `useMutation(api.agents.update)`. Show success feedback.
- The `/agents` page must have working "View Details" or "Open" button that navigates to `/agents/:id` (not opens a modal).
- Remove the old modal-based edit if "View Details" now navigates to detail page.
- Agents page: "Delete Agent" must NOT use `window.confirm`. Use inline confirmation (show "Are you sure? [Delete] [Cancel]" on the card).
- Run `pnpm test` — all 911+ tests must pass.
- Run `npx tsc --noEmit` in `packages/web` — zero TypeScript errors.

## Acceptance Criteria

- `/agents/agent-test-001` renders all 3 tabs with real data ✓
- Sessions tab shows real sessions for the agent ✓
- Settings tab saves changes and shows success ✓
- Agents list page navigates to detail page on click ✓
- No `window.confirm` anywhere in packages/web ✓
- All tests passing ✓
- Zero TypeScript errors ✓

## Implementation Notes

- Work in `/tmp/agentforge-team-b` on branch `feat/specs-003-004`
- `api.agents.get` already exists in `convex/agents.ts`
- `api.agents.update` already exists
- Add `sessions.listByAgent` to `convex/sessions.ts`: filter by `agentId` field
- Sync any new Convex functions to `packages/cli/templates/default/convex/`
- Use Radix UI `Tabs` already installed: `@radix-ui/react-tabs`
- Viewport: target 1440px wide (dashboard is NOT mobile-first)
