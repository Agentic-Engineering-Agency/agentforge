# SPEC-20260304-005: Integration QA & Session Detail Fix

**Status:** SPEC
**Created:** 2026-03-04
**Priority:** P0

## Summary

Fix remaining broken surfaces found in full end-to-end testing.

## Requirements

- Fix `sessions.$sessionId.tsx` so navigating to `/sessions/:id` renders the session detail page, not the sessions list
- Fix `agents.$agentId.tsx` so navigating to `/agents/:id` renders the agent detail page with Overview, Sessions, and Settings tabs
- Fix `sessions.endSession` Convex mutation to be deployed and callable
- Fix `agentforge sessions get <id>` so it returns session details and message preview from a real Convex session `_id`
- Fix `agentforge sessions delete <id> --force` so it accepts session IDs and returns appropriate error for missing sessions
- Fix `agentforge threads rename <id> <name>` so it persists the new name to Convex
- Fix `agentforge threads delete <id> --force` so it removes the thread and associated messages
- Verify Settings > Tokens tab in dashboard shows real tokens from Convex
- All 911+ existing tests pass without regression
- Zero TypeScript errors in packages/web

## Acceptance Criteria

- `GET /sessions/:id` renders session detail page with message history ✓
- `GET /agents/:id` renders agent detail with 3 tabs (Overview, Sessions, Settings) ✓
- `agentforge sessions get <real-id>` prints session details with message preview ✓
- Sessions (9) tab on agent detail shows real session rows ✓
- Settings > Tokens shows real tokens, Generate creates new token ✓
- All 911+ tests passing ✓
- Zero TypeScript errors ✓
